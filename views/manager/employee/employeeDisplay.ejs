<% include ../../navbar/admin %>

<div class="container">
  <div class="row">
    <div class="col s12">
      <div class="row">
        <h6>Manage Employee</h6>
        <h6 class="green-text" style="text-align: center"><% include ../../messages/success%></h6>
      </div>


      <div class="row">
        <div class="input-field col s12">
          <input id="searchInput" type="text" placeholder="search employee">
          <label for="searchInput">Search</label>
        </div>
      </div>

      <div class="row">
        <table class="highlight centered responsive-table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Employee ID</th>
              <th>Email</th>
              <th>Department</th>
              <th>Position</th>
              <th>Status</th>
              <th></th>
              <th></th>
            </tr>
          </thead>

          <tbody id="searchTable">
            <% employee.forEach((employee) => {%>
            <tr>
              <td><%= employee.employee_name %></td>
              <td><%= employee._id %></td>
              <td><%= employee.email %></td>
              <td><%= employee.department_name  %></td>
              <td><%= employee.position %></td>
              <td><%= employee.status %></td>
              <td>
                <a href="/manager/employee/display/personal-detail/<%= employee._id %>" class="black-text">
                  <i class="material-icons">folder_open</i></a>
              </td>
              <td>
                <a href="/manager/employee/delete/<%= employee._id %>" class="black-text">
                  <i class="material-icons">delete</i>
                </a>
              </td>
            </tr>
            <%})%>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div><!-- container -->